// to call a constructor need to call with constructor.

class Animal{
   constructor(){
        console.log("Default constructor");
    }
}

// to call a constructor just need to create a object
new Animal();


/* class Animal is created.
constructor() is a special method that runs automatically when an object is created.
new Animal() creates an object of the Animal class.
As soon as the object is created, the constructor executes and prints: */